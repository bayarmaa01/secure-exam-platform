apiVersion: v1
kind: Service
metadata:
  name: ai-proctoring
  namespace: exam-platform
spec:
  selector:
    app: ai-proctoring
  ports:
  - port: 5000
    targetPort: 5000
  - port: 8001
    targetPort: 8001
    name: metrics
  type: ClusterIP
  sessionAffinity: None
