apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: exam-platform
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
  - port: 80
    targetPort: 4000
    name: http
  - port: 9090
    targetPort: 9090
    name: metrics
